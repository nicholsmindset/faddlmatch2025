# 🔐 Clerk Authentication (REQUIRED)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key
CLERK_SECRET_KEY=your_clerk_secret_key
CLERK_WEBHOOK_SECRET=your_clerk_webhook_secret

# 🗄️ Supabase Database (REQUIRED)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# 🤖 OpenAI (optional - for AI features)
OPENAI_API_KEY=your_openai_api_key

# 💳 Stripe Payment Integration (REQUIRED)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key
STRIPE_SECRET_KEY=your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret

# 🌍 App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# 🔗 Clerk Custom Domain Configuration (Production)
# After DNS setup, update these for production:
# NEXT_PUBLIC_CLERK_DOMAIN=clerk.faddlmatch.com
# CLERK_ACCOUNT_PORTAL_URL=https://accounts.faddlmatch.com

# 🛡️ Security Configuration (Production)
# Set NODE_ENV=production for production deployment
NODE_ENV=development

# Rate limiting (default: enabled)
RATE_LIMIT_ENABLED=true

# Session security (milliseconds)
# Default: 86400000 (24 hours)
SESSION_MAX_AGE=86400000

# Webhook timestamp tolerance (seconds)
# Default: 300 (5 minutes)
WEBHOOK_TIMESTAMP_TOLERANCE=300

# 🚨 PRODUCTION SECURITY CHECKLIST:
# 1. Set strong, unique values for all secrets
# 2. Use HTTPS URLs in production (https://yourdomain.com)
# 3. Ensure CLERK_SECRET_KEY and CLERK_WEBHOOK_SECRET are 32+ characters
# 4. Enable rate limiting (RATE_LIMIT_ENABLED=true)
# 5. Set appropriate session timeout (SESSION_MAX_AGE)
# 6. Configure proper webhook tolerance (WEBHOOK_TIMESTAMP_TOLERANCE)
# 7. Set NODE_ENV=production
# 8. Review Content Security Policy in middleware.ts
# 9. Configure Stripe keys from your Stripe Dashboard
# 10. Set STRIPE_WEBHOOK_SECRET from Stripe webhook endpoint